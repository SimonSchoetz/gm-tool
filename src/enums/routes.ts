export enum Route {
  ERROR = '/error',
  HOME = '/',
  LOGIN = '/login',
  NOT_FOUND = '/not-found',
  PASSWORD_RESET = '/password-reset',
  SIGN_UP = '/sign-up',
  VERIFY_EMAIL = '/verify-email',
}
