# Spec Writer — Pre-Emission Compliance Pass

Three general gates over the complete spec:

1. **Convention compliance (outward)**: every file placement, import, naming choice, and structural decision satisfies the applicable rule in the owning scoped CLAUDE.md. The scoped file is the authority — re-read the specific rule rather than recalling it. Any detail that cannot be reconciled with CLAUDE.md must be corrected before emitting; any detail interpretable more than one way must be resolved or surfaced as an explicit question.
2. **Toolchain validity**: every code example passes the active compiler flags and ESLint plugin rules per `app/CLAUDE.md`'s TypeScript Coding Style toolchain-validity rule. Enumerate every flag in `app/tsconfig.json` compilerOptions — do not stop at the `strict` bundle; for non-strict-bundle flags, read `.claude/knowledge/typescript.md` before writing code touching the construct the flag governs.
3. **Internal consistency (inward)**: every code example agrees with every principle, KAD, and rule stated elsewhere in the same spec — including within the same section, not only across sections (e.g. a declared type shape contradicting prose describing behavior the type cannot express). Gate 1 is outward-only; it does not catch contradictions between two parts of the same spec.

Then check each specific gate. Each row earned its place by slipping past the general gates once — a hit is a spec defect to fix before emitting; the Authority column names the full rule.

| Gate | Trigger | Check | Authority |
| --- | --- | --- | --- |
| Import trace | any relative import in the spec | anchor first: sibling-inside-grouping-folder rule vs. barrel rule; then trace each `../` from the importer's actual file, not a feature root | app/src/CLAUDE.md — Barrel Files |
| Method-call narrowing | guard calls `f()`, body calls `f()` again | store `const result = f()`; use it in guard and body — tsc narrows variables, not call expressions | app/CLAUDE.md — TypeScript Coding Style |
| Always-true predicate | predicate on a value whose type already guarantees the tested case | remove it and use the narrowed value — `no-unnecessary-condition` fires on compile-time-provable guards | app/eslint.config.js |
| Removal claims | input asserts a construct is unneeded | no active ESLint rule independently requires it; unconfirmable removals are flagged to the user | app/eslint.config.js |
| Bare HTML elements | `<input>`, `<button>`, or typed variants (`<input type="color">`) in examples | glob `app/src/components/` for name-match and semantic-match wrappers; read the wrapper's props before use | app/src/CLAUDE.md — UI primitive wrappers |
| HTML content-model validity | spec'd JSX nests any element or component inside a native HTML parent element whose content model restricts permitted children (e.g. `<ul>`/`<ol>`, `<table>`, `<select>`) | verify the nested child's actual rendered output — not its wrapper component's name — against the parent's permitted-children list in the HTML Living Standard content model; read `.claude/knowledge/html.md` if the pairing is already cached, else fetch the standard and cache the verified pairing before relying on it; a component that itself renders the required element (e.g. an `<li>`-rendering wrapper) satisfies the pairing — a component that does not is a spec defect regardless of whether an existing codebase component nests loosely elsewhere | .claude/knowledge/html.md |
| Hook placement | any hook depending on a context provider | component renders below the provider — trace the tree, re-trace after moves | app/src/CLAUDE.md — providers/ |
| Helper extraction | a multi-statement body, or a single expression, reaches 2+ occurrences, including occurrences represented only via a substitution-table reference — trace the substitution table's reference file to determine the shape it produces rather than treating an un-reproduced body as invisible to this count | extract to `ComponentName/helper/` (multi-statement body) or to the nearest existing canonical location for the expression's layer (single expression); list every occurrence as a call site to update | app/src/CLAUDE.md — Component Library; root CLAUDE.md — duplicate-expression extraction |
| Sub-component extraction | near-identical JSX repeated 2+ times with only data-level variance | extract to `ComponentName/components/` | app/src/CLAUDE.md — Component Library |
| Memoization | any `useCallback`/`useMemo` in examples | qualifying reason stated at the call site (effect dependency or `React.memo` prop) — otherwise remove | app/src/CLAUDE.md — State Management |
| Async ownership | any Frontend layer entry places subscription/listener/async logic (e.g. `listen()`) in a component | cross-reference other Frontend entries and existing hooks for the same concern — screens and components own no async logic themselves | app/src/CLAUDE.md — TanStack Query pattern |
| Parent-type import | sub-component imports a type from its parent module | relocate the type to a neutral file both import from | app/src/CLAUDE.md — Component Library |
| Reference validity | any file cited as a reference implementation, discovered or named by an upstream agent | re-verify against current CLAUDE.md conventions — a reference's prior correctness is not assumed | root CLAUDE.md — Validate before replicating |
| Exemption citations | spec cites a rule's allowance clause to omit a file or barrel | trace the exemption's stated precondition against the proposed structure — pattern-recognition of a similar prior case is not verification | the cited rule itself |
| KAD call notation | KAD text contains `fn(args)` notation | SF body declares the extracted construct and its file, or notes inlining with reason | internal consistency |
| KAD boundary deviations | SF choice could look like a boundary-rule violation | inline rationale note at the decision site naming the KAD heading | app/docs/CLAUDE.md — SF self-containment |
| Test-per-path | KAD names 2+ distinct code paths | one named test per path; decision text is authoritative for test names | app/docs/CLAUDE.md |
| Singleton-state scaffolding | 2+ tests call a function owning module-level singleton state | `vi.resetModules()` in `beforeEach` + dynamic imports per test — "scaffolding unchanged" or static imports is wrong | app/db/CLAUDE.md — Testing |
| Tunable-constant assertions | tests of geometry/layout helpers driven by constants under visual tuning | assert from the same imported constant the implementation uses — never a baked literal (literals stay correct for non-tunable outputs) | app/src/CLAUDE.md — Testing Policy |
| toHaveBeenCalledWith arity | any such assertion | one matcher argument per call-site argument, in position order; the call site must be named or derivable from the spec | spec-internal call site |
| Insertion anchors | inserted code whose closure over a same-scope dependency is invoked before render completes (not deferred to an effect, handler, or other post-render callback) — via a named anchor or none | if an anchor is named, it appears after the dependency's declaration; if none is named, the spec states the required placement — deferred closures are exempt regardless of textual order; a direct (non-closure) same-scope read is already a tsc "used before declaration" error and out of this gate's scope | app/docs/CLAUDE.md — Insertion anchor validity |
| Existing-code claims | spec states a fact about an existing file's current syntax or structure (e.g. "already expression-bodied," "dep array is empty") | read the file in the current context window and confirm the claim before stating it — never state from pattern recognition or recall | root CLAUDE.md — Tool Use Discipline |
| Files-affected completeness | any file created, modified, moved, or read to derive a change in this sub-feature | confirm it appears under the sub-feature's Modified/New/Moved/Draft lists — including barrels, index files, and type files; for any import specifier a later SF names as a consumer, resolve it through every barrel it transits — grouping barrel and module barrel alike — not only the module barrel nearest the changed file | Output section — Files affected |
| Foundation annotations | any `[FOUNDATION]` SF | complete inline file list ("Stage as unit:"), no delegation to another SF's list | app/docs/CLAUDE.md |
| Analogous-member parity | the spec states a disposition, mechanism, or precision level (in prose or code) for one member of an analogous or mirrored set — two or more handlers, mirrored roles, normalization/conversion paths, or async chains | every other member of the set states its own disposition/mechanism/precision somewhere in the spec or carries an explicit waiver with a stated reason; silence next to a stated member reads as an omission, not an implicit "same as." For sets with cross-member state coupling (one handler/role reads state another writes): a state-transition table, one row per handler/role — triggering event, state read, state mutated, no-op conditions covering every item in that row's own state-mutated column. A member with no cross-member coupling and no analogous counterpart making a claim is exempt | spec-internal |
| Deprecated API reference | spec code names a third-party function or type that has more than one exported overload/signature | read the full declaration block — including JSDoc immediately above and below the matched signature — for `@deprecated` or other modifier tags; a matched signature line alone is not sufficient confirmation | spec-writer.md — Your Mandate (third-party library type-declaration read) |
| Fork-scan | any "or", "if needed", "if the implementer finds", or "may" appears in an implementation-detail sentence | resolve it against CLAUDE.md conventions and codebase patterns, or surface it to the user as an explicit question; resolving means naming every construct the resolved instruction applies to, not only the one nearest the sentence — a conditional attached to one construct's subsection does not implicitly extend to sibling constructs | spec-writer.md — Output ("Never use 'or', 'if needed', or 'may' for implementation details — resolve them") |
| CLAUDE.md impact vs. knowledge base | any entry drafted for the CLAUDE.md Impact section | classify it: a verified external-system fact (library API, schema value, CLI flag, config format) belongs in `.claude/knowledge/<topic>.md`, written directly, and must not appear in this section at all; only a convention or architectural gap belongs here | .claude/knowledge/CLAUDE.md — Write Obligation; spec-writer.md — Your Mandate |
| Untested residual risk | a construct is exempted from unit tests under the Testing Policy (React component) and its behavior falls in the category app/src/CLAUDE.md's Testing Policy names as untestable-by-policy residual risk (interaction-only behavior dependent on native browser default actions, invisible to helper/data-layer tests) | enumerate every risk in that category the construct carries — not only the first found — and state each one explicitly as its own manual-verification note in the sub-feature, prefixed with the literal marker `[MANUAL-VERIFY]` at the start of the line, naming the exact interaction to verify by hand; the Testing Policy exemption covers the absence of automated coverage, not the absence of risk, and covers it per risk, not per construct. The marker must appear verbatim, since `/implement`'s manual-verification risk scan greps for it deterministically | app/src/CLAUDE.md — Testing Policy |
| Third-party runtime testability verification | a construct's Testing Policy exemption is justified by the claim that a third-party runtime executes it (e.g. a transformer or rule configuration handed to an external tool) | verify against the installed package tree (`node_modules`, `package.json`) whether that runtime is drivable headlessly without a component render before accepting the exemption — configuration handed to a third-party runtime is still first-party surface, and its testability is a dependency question to check, not a conclusion to assume from which system executes it. If the runtime is drivable headlessly: write the automated test when the construct's directory falls within Testing Policy's scope (`helper/`, `/src/util/`); if the construct sits outside that scope despite being technically testable, do not default to `[MANUAL-VERIFY]` — record the scope gap in the CLAUDE.md impact section instead, since a confirmed-testable construct with no owning test obligation is a Testing Policy gap, not a residual risk | app/src/CLAUDE.md — Testing Policy |
| cn() static-argument misuse | any `cn()` call in spec code examples | at least one argument must depend on a runtime condition (ternary, `&&`, a variable) — two or more static strings with no conditional is a misuse | app/src/CLAUDE.md — Component Internals (cn() usage — conditional and computed classes only) |
| Ambient/global runtime type narrowing | spec code reads or coerces a property/method from a DOM or Node.js global object (e.g. `HTMLElement`, `Element`, `Node`, `process`) with an assumed nullable/optional signature | verify the signature at the most specific declaring interface in the actual inheritance chain (e.g. `HTMLElement`'s own getter, not `Node`'s) in the installed `lib.dom.d.ts`/`@types/node` — a base-interface signature does not apply once a subclass narrows it | app/CLAUDE.md — Third-Party Libraries (ambient/global runtime types) |
| Comment-formatting scan | any code block contains two or more consecutive `//` or `/** */`-continuation comment lines | grep the block for the pattern; determine whether the lines form a single sentence or paragraph manually split across them — rewrite as one unwrapped line before emitting — or are independently complete, separately punctuated statements stacked together (not a violation) | root CLAUDE.md — Coding style (manual line breaks in code comments) |
| Comment accuracy | a code comment states a factual claim that generalizes over more than one call path, case, or invocation (a contract comment, an invariant description, an "always"/"never" claim) | verify the claim against every case/call path it generalizes over, per the Pre-Emission Compliance Pass's population-inventory principle — a claim true for the case just implemented but false for a sibling case is a spec defect | root CLAUDE.md — Epistemological Discipline (verified evidence must cover the full scope of the claim) |
| Evidence-scope mismatch | KAD claim about a data value's permanence, exclusivity, or scope (e.g. "can never be tagged") is supported only by evidence of a narrower fact (e.g. a seed migration's one-time default) | confirm the evidence's scope matches the claim's scope — trace whether the value is exposed as live, user-editable state elsewhere (settings UI, update service) before treating a seed-time default as a permanent/structural boundary | root CLAUDE.md — Epistemological Discipline (Verified evidence must cover the full scope of the claim it supports) |
