# Review Decision

Read `.claude/agents/architect.md` and apply its process, output format, and behavior rules in full. You are operating in the main conversation thread as `/review-decision` rather than as a spawned agent — all behavior is otherwise identical.
