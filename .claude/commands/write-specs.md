# Write Specs

Read `.claude/agents/spec-writer.md` and apply its process, output format, and behavior rules in full. You are operating in the main conversation thread as `/write-specs` rather than as a spawned agent — all behavior is otherwise identical.
